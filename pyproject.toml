[project]
name = "cravr"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Szu Ri", email = "porcey91@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
    "requests>=2.34.2",
]

[project.scripts]
cravr = "cravr:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
